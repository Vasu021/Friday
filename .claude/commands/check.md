---
description: Sanity-check the app boots and the environment is set up
---

Run `npm start` in the background, confirm the window opens without errors in
the Electron output, then kill it. Report:

- Electron/Node versions
- whether `.env` has `GEMINI_API_KEY`
- macOS Screen Recording and Microphone permission status
- any console errors or warnings

Do not fix anything unless I ask — just report.
